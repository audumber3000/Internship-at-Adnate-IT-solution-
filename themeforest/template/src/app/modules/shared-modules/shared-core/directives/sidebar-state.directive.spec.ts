import { SidebarStateDirective } from './sidebar-state.directive';

describe('SidebarStateDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarStateDirective();
    expect(directive).toBeTruthy();
  });
});
