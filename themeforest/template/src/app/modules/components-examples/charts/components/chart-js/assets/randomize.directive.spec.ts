import { RandomizeDirective } from './randomize.directive';

describe('RandomizeDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomizeDirective();
    expect(directive).toBeTruthy();
  });
});
